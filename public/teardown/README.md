# Teardown screenshots

Drop product answer-state screenshots here, then pass the path to <TeardownImage src> in TeardownSection.tsx, e.g. src="/teardown/intercom-fin.png". Until a file exists, a styled placeholder renders automatically.
